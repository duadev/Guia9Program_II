@model Guia8Program_II.Models.Materia
@{
    ViewBag.Title = "Materias";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>

    <div class="row">
        <div class="card shadow">

            <body>
                <h3>Are you sure you want to delete this?</h3>
                <div>
                    <h4>Materia</h4>
                    <hr />
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.codMateria)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.codMateria)
                        </dd>

                        <dt>
                            @Html.DisplayNameFor(model => model.nombre)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.nombre)
                        </dd>

                        <dt>
                            @Html.DisplayNameFor(model => model.unidadValorativas)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.unidadValorativas)
                        </dd>

                    </dl>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <input type="submit" value="Delete" class="btn btn-default" /> |
                            @Html.ActionLink("Back to List", "Index")
                        </div>
                    }
                </div>
            </body>

        </div>

    </div>

</body>
</html>
